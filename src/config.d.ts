import { PlayerCallbacks, PlayerConfig, PlayerOptions } from './types';
export declare const DEFAULT_CALLBACKS: PlayerCallbacks;
export declare const DEFAULT_CONFIG: PlayerConfig;
export declare function createPlayerConfig(options: PlayerOptions): PlayerConfig;
